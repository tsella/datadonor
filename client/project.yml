name: DataDonor
options:
  bundleIdPrefix: la.tsel
targets:
  DataDonor:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    settings:
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    sources: [Sources]
    info:
      path: Info.plist
      properties:
        UILaunchScreen: {}
    entitlements:
      path: DataDonor.entitlements
